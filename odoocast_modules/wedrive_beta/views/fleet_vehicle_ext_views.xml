<odoo>
    <data>
        <record model="ir.ui.view" id="view_fleet_vehicle_kanban_ext">
            <field name="name">fleet.vehicle.kanban.ext</field>
            <field name="model">fleet.vehicle</field>
            <field name="inherit_id" ref="fleet.fleet_vehicle_view_kanban"/>
            <field name="arch" type="xml">
                <kanban>
                    <field name="license_plate"/>
                    <field name="model_id"/>
                    <field name="driver_id"/>
                    <field name="future_driver_id"/>
                    <field name="location"/>
                    <field name="state_id"/>
                    <field name="id"/>
                    <field name="tag_ids"/>
                    <field name="contract_renewal_due_soon"/>
                    <field name="contract_renewal_overdue"/>
                    <field name="contract_renewal_name"/>
                    <field name="contract_renewal_total"/>
                    <field name="contract_count"/>
                    <field name="activity_ids"/>
                    <field name="activity_state"/>
                    <progressbar field="activity_state" colors="{'planned': 'success', 'today': 'warning', 'overdue': 'danger'}"/>
                    <templates>
                        <t t-inherit="kanban-box" t-inherit-mode="extension">
                            <t t-set="has_license_plate" t-value="record.license_plate.raw_value"/>
                            <div class="o_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <t t-if="has_license_plate">
                                        <field name="license_plate"/>
                                    </t>
                                    <field name="model_id"/>
                                </strong>
                                <div class="o_kanban_bottom_left">
                                    <button name="action_get_plate_info" string="Consultar" type="object" class="oe_highlight"/>
                                </div>
                                <!-- Restante do código -->
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>
    </data>
</odoo>
